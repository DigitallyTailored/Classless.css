{
  "name": "classless",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cleancss -o classless.min.css classless.css"
  },
  "private": true,
  "devDependencies": {
    "clean-css-cli": "^5.6.3"
  }
}
